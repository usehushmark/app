export const sessionMarkup=`<div class="session-settings"><label for="auto-lock-minutes">Auto-lock after inactivity</label><select id="auto-lock-minutes"><option value="5">5 minutes</option><option value="15">15 minutes</option><option value="30">30 minutes</option></select><p class="field-help">An inactive session locks even during a payment. A submitted transaction may still complete; check its status after unlocking.</p></div>`;

export const backupMarkup=`<div class="journal-backup" id="activity-backup" hidden><h3>Keep an encrypted backup.</h3><p>Move your saved history to another browser with the same wallet. This file does not back up your funds or private notes.</p><div class="journal-toolbar"><button id="activity-export" class="secondary-button" data-action>Download backup</button><button id="activity-import" class="secondary-button" data-action>Restore backup</button><input type="file" id="activity-import-file" accept=".json,application/json" hidden></div><p id="backup-state" role="status" aria-live="polite"></p><div id="backup-review" class="journal-clear-confirm" hidden><h3>Restore this history backup?</h3><p id="backup-review-details"></p><p>Existing records are kept. New restored transaction statuses need a fresh network check.</p><div class="journal-toolbar"><button id="backup-confirm" class="secondary-button" data-action>Restore history</button><button id="backup-cancel" class="text-link">Cancel</button></div></div></div>`;

export const requestNotice=`<section id="incoming-request" class="incoming-request" hidden aria-labelledby="incoming-title"><p class="eyebrow">PAYMENT REQUEST</p><h2 id="incoming-title">Review the shared request.</h2><p id="incoming-details"></p><p id="incoming-expiry"></p><p>Check the recipient with the person who sent this link. Requests are not identity verification. The recipient amount excludes fees you will pay.</p><div class="journal-toolbar"><button id="request-use" class="secondary-button" data-action>Use this request</button><button id="request-dismiss" class="text-link">Dismiss</button></div><p id="request-incoming-state" role="status"></p></section>`;

export const walletToolsMarkup=`<section class="wallet-tools" aria-label="Payment tools"><section id="payment-request-section" class="tool-card"><p class="eyebrow">04 / SHARE A REQUEST</p><h2>Request a payment<span>.</span></h2><p>Create a link or QR code for a SOL or USDC payment. The recipient address is public.</p><label for="request-recipient">Recipient wallet address</label><input id="request-recipient" autocomplete="off" spellcheck="false" placeholder="Solana wallet address"><button id="request-own-wallet" class="text-link" type="button">Use connected wallet</button><div class="tool-fields"><div><label for="request-asset">Asset</label><select id="request-asset"><option>SOL</option><option>USDC</option></select></div><div><label for="request-amount">Recipient receives</label><input id="request-amount" inputmode="decimal" autocomplete="off" placeholder="0.00"></div></div><label for="request-duration">Link expires after</label><select id="request-duration"><option value="3600000">1 hour</option><option value="86400000" selected>24 hours</option><option value="604800000">7 days</option></select><button id="request-create" class="button" type="button">Create payment link</button><p id="request-create-state" role="status" aria-live="polite"></p><div id="request-result" hidden><img id="request-qr" alt="QR code containing this Hushmark payment request" width="260" height="260"><label for="request-link">Payment link</label><input id="request-link" readonly><div class="journal-toolbar"><button id="request-copy" class="secondary-button">Copy link</button><button id="request-download" class="text-link">Download QR</button></div><p>Anyone with the link can read the address and amount. The payer reviews fees and confirms separately. A request does not prove payment.</p></div></section><section id="service-section" class="tool-card"><p class="eyebrow">05 / CHECK AVAILABILITY</p><h2>Service status<span>.</span></h2><p>Check the selected RPC, privacy program and protocol fee service without connecting a wallet.</p><dl class="service-list">${[['rpc','Solana RPC'],['program','Privacy program'],['relayer','Protocol fee service']].map(([id,label])=>`<div><dt>${label}</dt><dd><span id="service-${id}-status" class="service-badge" data-state="unknown">Not checked</span><p id="service-${id}-detail">Run a check to read current availability.</p></dd></div>`).join('')}</dl><button id="service-check" class="secondary-button">Check services</button><p id="service-check-state" role="status" aria-live="polite">No checks have run yet.</p><p class="field-help">Availability is a point-in-time check, not a security audit or a promise that a payment will succeed. Checks send read requests to your RPC and Privacy Cash.</p></section></section>`;

export const walletToolsDocs=`<section id="backup"><h2>Back up your activity history.</h2><p>While unlocked, choose Download backup to save an encrypted JSON file. Restore it with the same software wallet on another browser or domain. Hushmark validates and merges the file locally, keeps existing records and retains the latest 100. Conflicting or corrupted files are rejected without replacing your history. New restored records are marked unverified until you check their network status. Backup files contain a public wallet address and encrypted history; no seed phrase, unlock signature or private-note spending key is exported.</p><p>This is a backup of activity records only. It does not recover funds, replace your original wallet or restore private notes. Keep the original software wallet for protocol recovery. No history file is uploaded to a backend.</p></section><section id="sessions"><h2>Sessions expire when idle.</h2><p>Auto-lock defaults to five minutes and can be changed to fifteen or thirty. It clears unlocked history, review details and in-memory keys when you stop interacting. Background tabs are checked again on return and before guarded actions. Auto-lock can interrupt proof generation or a pending wallet prompt. A transaction already sent may still complete; the last saved uncertain status stays available after unlocking, so check it before retrying.</p></section><section id="requests"><h2>Request a payment by link or QR.</h2><p>Create a SOL or USDC request with a recipient, net amount and expiry. Links use the website's own wallet page and keep details in a URL fragment. The address and amount are readable by anyone who has the link. The request is not signed identity verification, a stealth address, a private invoice or proof of payment.</p><p>The payer unlocks the wallet, explicitly chooses Use this request, then reviews the amount debited, current protocol fee and exact recipient amount. Hushmark calculates the gross withdrawal needed for that amount. Editing the payment fields detaches the shared request; expired requests cannot authorize a new payment. Opening a link never submits a transaction, and no automatic Paid status is claimed.</p></section><section id="services"><h2>Read current service availability.</h2><p>Check services makes read-only requests to the configured RPC and Privacy Cash's fee endpoint. It verifies Solana mainnet, a finalized slot, the executable privacy program and valid fee configuration. Results include the check time and can become stale. An available endpoint is not an audit, deposit guarantee, or verification that the relayer can complete a withdrawal. Changing RPC clears old results. No automatic provider switching or transaction retry occurs.</p></section>`;
